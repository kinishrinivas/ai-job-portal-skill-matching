# .env.example - Example environment variables file
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git (it contains passwords!)

# ===== FLASK SETTINGS =====
SECRET_KEY=your-super-secret-key-here-change-this
FLASK_DEBUG=True

# ===== MONGODB SETTINGS =====
# For local development (MongoDB installed on your computer):
# MONGO_URI=mongodb://localhost:27017/job_portal_db

# For MongoDB Atlas (cloud database - we'll use this):
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/job_portal_db?retryWrites=true&w=majority

DATABASE_NAME=job_portal_db

# ===== CORS SETTINGS =====
# Frontend URL (where React runs)
CORS_ORIGINS=http://localhost:3000

# ===== EMAIL SETTINGS (Optional - for future) =====
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# ===== INSTRUCTIONS =====
# 1. Copy this file and rename it to .env
# 2. Replace the placeholder values with your actual credentials
# 3. For MongoDB Atlas:
#    - Create free account at https://www.mongodb.com/cloud/atlas
#    - Create a cluster
#    - Get connection string and replace MONGO_URI
# 4. Generate a random SECRET_KEY:
#    - In Python: import secrets; print(secrets.token_hex(32))
